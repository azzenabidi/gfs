{
  "name": "gfs-clone-explorer-pro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "description": "Richer SOURCE vs GFS CLONE explorer: multi-table schema (uuid/bigint/composite keys), joins, fuzzy search, temporal filters, an aggregate dashboard, and copy-on-write writes — with an optional auto-warming proxy.",
  "scripts": {
    "demo": "bash scripts/run.sh",
    "build:web": "pnpm --filter clone-explorer-pro-web run build",
    "server": "pnpm --filter clone-explorer-pro-server run start"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
