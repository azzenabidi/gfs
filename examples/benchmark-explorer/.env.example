# TPC-H scale factor (the source is generated by DuckDB and PERSISTS in a named
# volume per SF). SF=1 ~1GB (proof), SF=10 ~16GB, SF=50 ~100GB.
SF=1

# Host ports.
SOURCE_PORT=55620
CLONE_PORT=55621
SERVER_PORT=8789

REMOTE_HOST=host.docker.internal
